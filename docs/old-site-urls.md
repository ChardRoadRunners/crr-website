# Old Webador URLs

Captured **19 September 2026** from `https://chard-road-runners.webador.co.uk/sitemap.xml`,
while the old site was still up.

`docs/launch-checklist.md` asks for this before the old site is cancelled on
30 September 2026. Once it is gone the list cannot be recovered, and every link
anybody has ever shared breaks with nothing to redirect it to. This is that
list — the capture, not the redirects.

## The eleven pages

All eleven, exactly as the old sitemap gave them. The paths are what matter;
the host changes when the domain moves.

| Old path | Obvious destination |
| --- | --- |
| `/` | `/` |
| `/contact` | `/contact` |
| `/pub-runs` | `/pub-runs` — slug kept deliberately |
| `/our-races` | `/our-races` |
| `/race-calendar` | `/calendar` |
| `/boring-legal-stuff/rules-and-constitution` | `/rules-and-constitution` |

## The five that need a decision

Not guessed here, because a redirect sending somebody to the wrong page is
worse than one that does not exist:

| Old path | Candidates | The question |
| --- | --- | --- |
| `/our-races/chard-flyer` | `/our-races` | The new site has no per-race page, so this can only land on the combined one. Worth an anchor if the two races ever get their own sections. |
| `/our-races/forde-abbey-10k` | `/our-races` | Same. |
| `/club-championship` | `/results#championship` | `/results` has a `championship` anchor already, so this one is nearly settled — confirm the championship is what that page now means. |
| `/membership-form` | `/join-us` | Join Us covers joining, but the old page was the form itself. If the form still lives somewhere, it may want to point there instead. |
| `/boring-legal-stuff` | `/welfare`, `/privacy`, `/inclusion`, `/rules-and-constitution` | The old site had one legal index; the new one has four separate pages and no index. Needs somebody to pick the landing page, or a small index page. |

## Also captured

The old `/pub-runs` page said this, in full. Recorded because it is the only
written description of a pub run the club had, and it goes when the site does:

> We aim to hold a monthly pub run during the summer starting and ending at a
> different pub each month.
>
> Dates are:- Wednesday 3rd July / Thursday 15th August / Thursday 5th
> September (head torches may be needed for thus one)
>
> contact us for more info.

No year was given. Note that two of the three dates are Thursdays, which does
not match "once a month on a Wednesday" — worth checking with the social
secretaries which it is, though the calendar now carries the real dates either
way.

The old navigation put **Pub Runs under Our Races**. The new site puts it in
the footer instead, since Our Races means the two races the club hosts.
